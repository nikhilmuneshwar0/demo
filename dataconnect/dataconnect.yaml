specVersion: "v1"
serviceId: "demo"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "demo-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
